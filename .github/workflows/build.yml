name: Build and Package App with PyInstaller

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  build-binaries:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # FIX 1: Downgrade Ubuntu runner to prevent GLIBC_2.38 requirement
        os: [windows-latest, macos-latest, ubuntu-20.04] 
        # FIX 1: Using the currently supported LTS runner for Linux stability
        os: [windows-latest, macos-latest, ubuntu-22.04] 

    steps:
      - name: Checkout Repository
@@ -36,16 +36,17 @@
            pyinstaller App_PP.py --noconsole --onefile --distpath build/windows --name Kaalen-v1.0 --icon icon.ico
          elif [ "${{ matrix.os }}" == "macos-latest" ]; then
            pyinstaller App_PP.py --windowed --onefile --distpath build/macos --name Kaalen-v1.0 --icon icon.ico
          elif [ "${{ matrix.os }}" == "ubuntu-20.04" ]; then # Update OS matrix reference
          elif [ "${{ matrix.os }}" == "ubuntu-22.04" ]; then # Updated condition
            pyinstaller App_PP.py --onefile --distpath build/linux --name Kaalen-v1.0
            # FIX 2: Ensure executable permission is set
            # FIX 2: Explicitly set executable permission for Linux binary 
            chmod +x build/linux/Kaalen-v1.0
          fi
        shell: bash

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          # The name must match the matrix OS tag
          name: Kaalen-${{ matrix.os }}
          path: build/*
          retention-days: 7
@@ -75,7 +76,7 @@
      - name: Zip Linux artifact
        run: |
          # FIX 3: Update artifact directory name to match the new runner name
          cd artifacts/Kaalen-ubuntu-20.04 
          cd artifacts/Kaalen-ubuntu-22.04 
          zip -r ../../Kaalen-linux.zip .

      - name: Create Release
